#include<stdio.h>
int main() 
{
    int row,col,sum=0;
    scanf("%d %d",&row,&col);
    int arr1[row][col]; 

    for(int i=0;i<row;i++)
    {
        for(int j=0;j<col;j++)
        {
            scanf("%d",&arr1[i][j]);
        }
    }

    for(int i=0;i<row;i++)
    {
        for(int j=0;j<col;j++)
        {
            if(i==j)
            {
                if(arr1[i][j]==1)
                {
                    sum++;
                }
            }
        }
    }
    
    if(sum==row)
    {
        printf("identity matrix");
    }
       
    else
    {
        printf("non identity matrix");

    }
}
