#include<stdio.h>
int main()
{
    int row,col;
    scanf("%d%d", &row,&col);
    int arr[row][col];
    for(int i=0;i<row;i++) 
    {
        for(int j=0;j<col;j++)
        {
            scanf("%d", &arr[i][j]);
        }
    }
   
    for(int i=0;i<row;i++)
    {
        for(int j=i+1;j<col;j++)
        {
            if(arr[i][j]!=arr[j][i] )
            {
                printf("Not a Symmetric Matrix");
                return 0;
            }
            else
            {
                printf("Symmetric Matrix");
                return 0;
            }
        }
    }
   
    return 0;
}
